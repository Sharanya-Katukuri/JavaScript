// Find the absolute value of -25 using a Math method.
console.log(Math.abs(-25))

// Round the number 4.7 to the nearest integer using a Math method.
console.log(Math.round(4.7))

// Round 4.2 downward to the nearest integer using a Math method.
console.log(Math.floor(4.2))

// Round 4.9 upward to the nearest integer using a Math method.
console.log(Math.ceil(4.9))

// Find the maximum number among 15, 27, 9, 42.
console.log(Math.max(15,27,9,42))

// Find the minimum number among 5, 3, 9, 1.
console.log(Math.min(5,3,9,1))

// Generate a random number between 0 and 1 using a Math method.
console.log(Math.random())

// Generate a random integer between 1 and 10.
console.log(Math.floor(Math.random()*10)+1)

// Find 2 raised to the power of 5 using a Math method.
console.log(Math.pow(2,5))

// Find the square root of 81 using a Math method.
console.log(Math.sqrt(81))

// Use Math.trunc() to remove the decimal part of 9.876.
console.log(Math.trunc(9.876))

// Use Math.sign() to check the sign of -25.
console.log(Math.sign(-25))
console.log(Math.sign(56))
console.log(Math.sign(0))
console.log(Math.sign("name"))

// Find the cube root of 27 using a Math method.
console.log(Math.cbrt(27))

// Use a Math method to generate a random integer between 1 and 10.
console.log(Math.floor(Math.random()*10)+1)

// Find the absolute difference between two numbers x = -15 and y = 8 using a Math method.

var x=-15
var y=8
console.log(Math.abs(x-y))

// Find the largest integer less than or equal to 7.99 using a Math method.
console.log(Math.floor(7.99))

// Find the smallest integer greater than or equal to 3.14 using a Math method.
console.log(Math.ceil(3.14))

// Find the square of 12 using a Math method.
console.log(Math.pow(12,2))

// Round 5.49 to the nearest integer using a Math method.
console.log(Math.round(5.49))

// Find the value of π (pi) using a Math property.
console.log(Math.PI)

// Find the natural logarithm (base e) of 10 using a Math method.
console.log(Math.log(10))

// Find the logarithm of 100 (base 10) using a Math method.
console.log(Math.log10(1000))

// Find the exponential value of 2 (that is, 𝑒^2) using a Math method.
console.log(Math.exp(2))

// Find the highest number from the list 45, 67, 12, 98, 23 using a Math method.
console.log(Math.max(45,67,12,98,23))

// Find the lowest number from the list 34, 7, 89, 2, 56 using a Math method.
console.log(Math.min(34,7,89,56))

// Use a Math method to find the sine value of an angle (in radians) — for example, π / 2.
console.log(Math.sin(Math.PI/2))

// Find the cosine value of 0 radians using a Math method.
console.log(Math.cos(0))

// Find the tangent value of π / 4 using a Math method.
console.log(Math.round(Math.tan(Math.PI/4)))

// Use a Math method to find the absolute value of -99.9.
console.log(Math.abs(-99.9))

// Use a Math method to find the cube of the number 5.
console.log(Math.pow(5,3))